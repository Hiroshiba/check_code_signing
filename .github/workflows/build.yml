name: Build

on:
  push:
    branches:
      - main

jobs:
  hoge-job:
    runs-on: windows-latest
    environment: hoge
    steps:
      - uses: actions/checkout@master

      - shell: bash
        run: |
          echo ok
          echo secret "${{ env.HOGEENV }}"
        env:
          HOGEENV: ${{ secrets.HOGEENV }}
